package com.example.buildPlugin

import com.example.buildPlugin.dependencies.*

object Dependencies {

    val config = Config
    val compose = Compose
    val di = DI
    val retrofit = Retrofit
    val room = Room
    val test = Test
    val other = Other
}